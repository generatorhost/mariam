# Architecture Library Pointer

All new Mariam code must follow the official architecture library:

https://github.com/generatorhost/Mariam-Architecture-Library

Implementation is allowed only when it maps to documented layers, contracts, runtime objects, plugin/app manifests, data boundaries, APIs, permissions, tests, and acceptance criteria.

If code and documentation conflict, the documentation is corrected first or the code is rejected.

